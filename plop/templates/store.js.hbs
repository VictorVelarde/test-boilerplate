/**
 *  {{pascalCase name}} store module file
**/

import { state } from './state'
import { mutations } from './mutations'
import { actions } from './actions'
import { getters } from './getters'

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}

export { MODULE_NAME as MODULE_NAME_{{constantCase name}}, ACTIONS as ACTIONS_{{constantCase name}}, GETTERS as GETTERS_{{constantCase name}}, MUTATIONS as MUTATIONS_{{constantCase name}} } from './constants'
